TODO Добавить роль анонимус
    добавить роль в таблицу(и скрипт ддл), модифицировать метод UserServiceImpl.save() на большее кол-во ролей(конкретно строку setRoles)
TODO добавить enum для ролей
TODO Если пытаться зарегистрировать юзера с уже существующим логином - ошибка 500. Пофиксить
TODO Убрать логгирование
TODO Зарегистрированный пользователь может попасть на страницу /login?error. Так быть не должно
TODO Добавить страничку forgot password
TODO Добавить поле при регистрации
    https://github.com/hellokoding/registration-login-spring-xml-maven-jsp-mysql/commit/b399fc333ecb4646d6ea30d6a23018ab2b671047


Question UserController.registration(): @ModelAttribute("userForm") что такое?
Question Как так получается, что пароли все одинаковые(12...8), а после кодировки - разные?
Question UserController.registration(): method = RequestMethod.GET чем отличается от method = RequestMethod.POST и др?
    может помочь: Login POST request is handled/provided by Spring Security
Question UserController.login(): что такое Model, что значит model.addAttribute(...)?
Question Почему мы не помечаем интерфейсы dao аннотацией @Repository?
Question appconfig-mvc.xml 14строка, почему если я изменю название файла, spring его не найдет


INFO if i want to change default page to home.jsp instead of login.jsp then 1)Remove 11th line in application-security.xml 2)Clone another controller for home.jsp in UserController
INFO Чтобы, например, удалять юзеров нужно добавить соотвествующий метод delete(...) в UserService и UserServiceImpl